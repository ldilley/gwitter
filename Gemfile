source 'http://rubygems.org'

fail "Ruby >=2.2 is required! Detected version: #{RUBY_VERSION}" unless RUBY_VERSION.to_s >= '2.2'

gem 'rake', '>= 10.1.0'
gem 'sinatra', '>= 2.0.1'
gem 'twitter', '>= 6.2.0'
